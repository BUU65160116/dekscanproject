<!doctype html>
<html lang="th">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>สมัครสมาชิก</title>
  <link rel="stylesheet" href="/register.css">

  <style>
  
  </style>
</head>
<body>
  <main class="wrap">
    <section class="card">
      <h1 style="margin:0 0 8px">สมัครสมาชิก</h1>
      <p class="muted">กรอกชื่อและเบอร์เพื่อสมัคร</p>

      <% if (error) { %><div class="msg err"><%= error %></div><% } %>
      <% if (msg)   { %><div class="msg ok"><%= msg %></div><% } %>

      <form action="/register" method="post" novalidate>
        <input type="hidden" name="shopCode" value="<%= shop %>">
        <input type="hidden" name="tableNumber" value="<%= table %>">

        <div class="field">
          <label for="name">ชื่อ</label>
          <input id="name" name="name" type="text" placeholder="เช่น นัทธการ" required />
        </div>
        <div class="field">
          <label for="phone">เบอร์โทร</label>
          <input id="phone" name="phone" type="tel" inputmode="tel" placeholder="เช่น 0812345678" required />
        </div>

        <button class="btn" type="submit">สมัคร</button>
      </form>

      <p class="muted" style="margin-top:14px">
        มีบัญชีแล้ว? <a href="/login?<%=
          `shop=${encodeURIComponent(shop||'')}&table=${encodeURIComponent(table||'')}`
        %>">กลับไปล็อกอิน</a>
      </p>
    </section>
  </main>
</body>
</html>
