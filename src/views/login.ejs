<!doctype html>
<html lang="th">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ล็อกอินเช็คอินโต๊ะ</title>
  <link rel="stylesheet" href="/login.css">
  <style>
   
  </style>
</head>
<body>
  <main class="wrap">
    <section class="card">
      <h1 style="margin:0 0 8px">ล็อกอินเช็คอินโต๊ะ</h1>
      <p class="muted">กรอกเบอร์ที่เคยสมัครไว้</p>

      <% if (error) { %><div class="msg err"><%= error %></div><% } %>
      <% if (msg)   { %><div class="msg ok"><%= msg %></div><% } %>

      <form action="/login" method="post" novalidate>
        <input type="hidden" name="shopCode" value="<%= shop %>">
        <input type="hidden" name="tableNumber" value="<%= table %>">

        <div class="field">
          <label for="phone">เบอร์โทร</label>
          <input id="phone" name="phone" type="tel" inputmode="tel" placeholder="เช่น 0812345678" required />
        </div>

        <button class="btn" type="submit">ยืนยันเช็คอิน</button>
      </form>

      <!-- ลิงก์ไปสมัคร พร้อมพารามิเตอร์เดิม -->
      <p class="muted" style="margin-top:14px">
        ยังไม่เคยสมัคร? <a href="/register?<%=
          `shop=${encodeURIComponent(shop||'')}&table=${encodeURIComponent(table||'')}`
        %>">สมัครเลย</a>
      </p>
    </section>
  </main>
</body>
</html>
