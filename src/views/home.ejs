<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Home • <%= shop %></title>
  <link rel="stylesheet" href="/home.css" />
</head>
<body>
  <div class="container">
    <h2>สวัสดี <%= user.Name %> • <%= shop %></h2>

    <div class="card">
      <p><strong>เบอร์โทร:</strong> <%= user.PhoneNumber %></p>
      <p><strong>โต๊ะ:</strong> <%= table %></p>
      <p><strong>แต้มสะสม:</strong> <span class="points"><%= points %></span></p>

      <% if (checkedInToday) { %>
        <p><span class="chip chip-ok">เช็คอินวันนี้แล้ว (+1)</span></p>
      <% } else { %>
        <p><span class="chip chip-warn">ยังไม่ได้เช็คอินวันนี้</span></p>
      <% } %>
    </div>

    <div class="row">
      <a class="btn btn-primary" href="/points">ดูรายละเอียดแต้ม</a>
      <a class="btn btn-secondary" href="/warp">แจกวาปขึ้นจอ</a>
      <a class="btn btn-secondary" href="/chat">เข้าแชทเรียลไทม์</a>
      <a class="btn btn-danger" href="/logout">ออกจากระบบ</a>
    </div>
  </div>
</body>
</html>
